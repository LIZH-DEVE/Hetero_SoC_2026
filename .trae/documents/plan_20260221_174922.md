## SM4密钥扩展修复计划

### 问题分析
1. **CK参数索引不匹配**: `get_cki`使用`count_round`，而 `one_round_for_key_exp`使用`reg_count_round`， 导致CK参数与轮计算不同步

2 **修复**: 统一使用 `reg_count_round`

2 **轮密钥存储时序**: 轮密钥存储使用 `data_after_round`（组合逻辑输出)
   需要在正确的时钟边沿捕获

### 修复步骤
1. 修改 `get_cki` 实例化使用 `reg_count_round`
2. 修复轮密钥存储条件，3. 运行测试验证